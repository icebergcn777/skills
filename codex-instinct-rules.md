# Cowork Instincts → Codex 规则同步

> 🔄 全量同步于 2026-08-24 | 源：Cowork instinct-store.md + memory-decay-rules.md
> 自动同步 Skill：`/sync-instincts`
> 用途：粘贴到 Codex 的 AGENTS.md 或项目级 AGENTS.md 中
> 当源文件更新时，本文件会被全量覆盖式同步。

---

## 通用规则

以下规则不依赖特定 AI 工具。Codex / Claude Code / Cursor / Copilot 均可遵循。

### R1. 全流程分析走三级递进

**场景：** 需要深度分析时（股票分析、项目评估、商业决策）
**规则：** 走 L1(速查) → L2(定量) → L3(尽调+对抗审查) 三级流水线，不跳过中间验证环节
- L1：快速扫描基本面、行情事件、关键指标
- L2：估值建模、对标分析、量化评分
- L3：多角度压力测试、认知谬误检测、交叉验证 + 置信度评分

### R2. 内容创作标准流水线

**场景：** 写文章、公众号、小红书、视频文稿
**规则：** 按序执行 写作 → 去AI味（删套路化表达）→ 排版适配目标平台 → 配封面图 → 发布
**注意：** 每一步输出是下一步的输入，不要跳步

### R3. 涨停分析三步流程

**场景：** 分析 A 股涨停股
**规则：** 走三步全部完成后再出结论
1. 竞价评分 + 涨停模式识别（是什么类型涨停？量价配合？）
2. 风险排查（查减持公告、近期利空、龙虎榜席位）
3. 量价确认（量柱形态、量线支撑压力位、量波走势验证）

### R4. 任务编排原则

**场景：** 安排多个子任务时
**规则：**
- 先检查任务间是否有依赖关系
- 有依赖 → 串行跑，前一产出作为后一输入
- 无依赖 → 独立任务并行执行
- 切换到新任务前确认当前任务已完结
**反例：** 所有任务揉在一起让 AI 同时做、做到一半切出去

### R5. 置信度衰减原则（元规则）

**场景：** AI 记住的规则/事实需要判断可信度
**规则：**
- 每条规则/事实带置信度分数和最后验证时间
- 超过 30 天未验证的规则，置信度按半衰期衰减一半
- 有效分 ≥ 0.7 直接采纳，0.2-0.49 标记为"旧信息"，< 0.2 跳过
- 当前任务匹配规则领域时（如分析股票匹配交易规则），可提升衰减记忆的优先级

### R6. 项目隔离

**场景：** 跨项目工作时
**规则：** 不同项目积累的规则/偏好默认隔离。React 项目的命名习惯不自动应用于 Python 项目。同一模式在 2+ 个项目中同时出现时，提升为通用规则。

### R7. Instinct 定期导出

**场景：** instinct-store 有新增/变更时
**规则：** 出现新规则或已有规则置信度跨越阈值时，同步更新两个导出文件
**注意：** Cowork 中用 `/sync-instincts` 自动执行；Codex 中手动复制本文件内容到 AGENTS.md

### R8. 交付即自检（三层验证纪律）

**场景：** 报告任何程序/分析"完成"之前
**规则：** 报告完成之前，必须证明产出是正确的。程序不崩溃 ≠ 程序正确；算出来的数对不对才是交付标准。验证责任在产出方，不推给用户。
- 第1层 自验证：产出数据本身带语义校验（整列全零→链路断裂、金额入比率列→字段错选、维度不一致→特征被静默丢弃）
- 第2层 多 Agent 对抗审查：重大改动用对立视角横向扫描同类 bug（未来泄漏/静默降级/索引错位/字段错选）
- 第3层 验证后再交付：改动代码必须真实数据端到端跑一遍，含子进程/并行/SSE，才算"完成"
- 验收清单 ≥2 个 ❌ → 不得交付

### R81. 会话隔离

不同任务开新对话。不清零旧历史就带着走 → 新消息只占整包的不到1%。交接用文档（四件事：目标/进度/限制/完成标准），不复制聊天记录。

### R82. 回车前三问

每次按回车前自问：
1. 新任务真的需要前面历史吗？→ 不需要则开新对话
2. 需要全部吗？→ 大多数只要结论，整理成文档带过去
3. 模型/配置开工定好了吗？→ 定好就别动，让缓存安心打一折

### R83. 批量改规则

会话中途改任何规则文件 = 前缀字节变 = 全量重算。攒 3-5 条一起改，改完开新会话。

### R84. 模型按任务分级

强模型（Opus/Fable）做判断（架构/方案/模糊需求/安全审查/最终验收），稳定基底（Haiku/Sonnet/DeepSeek Flash）做执行（搜索/整理/跑测试/批量修改）。不要所有任务都上最强（浪费），也不要所有都用最弱（理解力不够反复返工更贵）。

### R85. MCP 开工前定好

工具清单排在整包最前面（上万 token）。中途加/减 MCP = 抽掉第一张多米诺 = 缓存全灭。所有 MCP 在开工前配齐。

### R86. Agent 等待纪律

子代理跑长任务时，不反复轮询（每隔20秒检查一次 = 反复叫醒总控 = 白白烧 token）。策略优先级：①原生监听/事件通知 ②sleep固定时间 ③条件等待 ④退避轮询仅最后手段（15s→30s→60s→2min，每次只读最小状态）。

### R87. 验收标准前置

短 prompt ≠ 省 token，能减少返工的 prompt 才真省。发任务时写清：①目标 ②哪些可改/不能动 ③怎么判断完成 ④什么情况停止并问用户。

### R88. 命令精简 + @符号注入（官方技巧）

**来源：Claude 官方博客（2026-08-24）**

1. **命令精简**：把常用命令精简版写进 CLAUDE.md/AGENTS.md
   - `git log` → `git log --oneline`
   - `npm run test` → `npm run test -- --silent`

2. **@符号注入**：指定文件用 @ 符号直接注入内容，省掉 AI 搜索+读取两轮
   - 例：`@utils.test.ts 修一下`（而非 "修复 utils.test.ts 中的问题"）

两者叠加，单次任务可省 30-50% 上下文。

---

## Cowork 专属规则（其他环境忽略）

以下规则依赖 Cowork chat 环境特有功能，导出仅为完整记录：

- ~~找 Skill 用控制台面板，不翻目录~~ — 依赖 Cowork artifact 机制
- ~~observer-protocol 四类观测触发~~ — 依赖 AI 在 chat 中主动观测

---

## 同步记录

| 同步时间 | 源版本 | 变更内容 |
|---------|--------|---------|
| 2026-07-10 | 初始 | instinct-store 6条 + memory-decay-rules R5/R6 全量导出 |
| 2026-07-10 | 自动同步 v2 | 新增 R7 (instinct-export-to-shared) |
| 2026-07-19 | 自动同步 v3 | instinct-store 不变，COWORK-CODEX-RULES.md 重构为 19 条完整版本 |
| 2026-08-04 | 手动同步 v4 | 新增 R8 交付即自检 |
| 2026-08-24 | 手动同步 v5 | 新增 R81-R88 Token经济纪律（三篇视频整合：会话隔离/回车三问/批量改规则/模型分级/MCP固定/Agent等待/验收标准/命令精简） |
