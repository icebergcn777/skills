---
name: md-to-html
description: Markdown → 公众号/知乎/HTML 排版，支持代码块/数学公式/外链转文末引用/多主题切换
---

# md-to-html 排版 Skill

## 功能概述
将 Markdown 文章转换为可直接粘贴到微信公众号编辑器、知乎、或独立 HTML 页面的排版。
核心原则：**保持原 Markdown 语义结构，输出干净、专业的排版 HTML。**

---

## 一、使用方式

直接说"排版这篇文章"或"转成公众号格式"，系统会自动检测输入格式。也可以指定：

```
/排版 这篇文章 用 极简主题
/排版 用 暖色主题 模式:公众号
```

### 主题列表
| 主题名 | 风格 | 适用场景 |
|:------|:----|:--------|
| 极简（默认） | 黑白灰，干净留白 | 技术文章、深度分析 |
| 暖色 | 米白底+棕色文字 | 个人随笔、故事 |
| 冷色 | 深蓝+银灰+科技感 | 数据报告、行业分析 |
| 暖阳 | 浅金+橙+阅读感 | 公众号长文 |
| 暗夜 | 深色背景+白字 | 代码分享、夜间阅读 |

---

## 二、核心排版规范

### 2.1 基础规则
- 输出**纯内联样式 HTML**（公众号编辑器只认内联 style）
- 每一段放在 `<section>` 标签内，禁止使用 `<div>` 被编辑器吃掉
- 字号基准 16px，行高 1.75，确保移动端阅读舒适
- 段落间距 12px，拒绝大段文字堆叠

### 2.2 Markdown 元素映射

| Markdown | HTML 输出 |
|:---------|:----------|
| `# 标题` | `<h1 style="font-size:22px;font-weight:700;...">` |
| `## 标题` | `<h2 style="font-size:18px;font-weight:600;...">` |
| `> 引用` | `<blockquote style="border-left:3px solid #xxx;...">` |
| `` `代码` `` | `<code style="background:#f0f0f0;padding:2px 6px;...">` |
| ` ``` ` 代码块 | `<pre><code>` + 语法高亮 |
| `- 列表` | `<ul><li>` |
| `1. 列表` | `<ol><li>` |
| `**粗体**` | `<strong>` |
| `[链接](url)` | 正文链接→蓝色可点击；外链→文末引用标注 |
| `![图片](url)` | `<img>` 自适应宽度，加圆角 |
| `$$` 数学公式 | `<span class="katex">` 或图片降级 |

### 2.3 特殊元素
- **提示框 Callout**：`> [!NOTE]` / `> [!WARNING]` / `> [!TIP]` → 带图标和背景色的卡片
- **表格**：`<table>` 加交替行背景，首行加粗
- **步骤列表**：自动生成带数字标号的大步骤卡片风格

### 2.4 外链处理
- 正文中的超链接保持可点击（蓝色+下划线）
- 同时自动在文末生成**引用列表**：`[1] 链接标题: URL`
- 避免公众号外链无法打开的尴尬

### 2.5 排版红线
- ❌ 禁止大圆角卡片（公众号编辑器渲染不一致）
- ❌ 禁止 flex/grid 布局（编辑器不支持）
- ❌ 禁止外部 CSS 文件
- ❌ 禁止色彩超过 3 种（正文色 + 强调色 + 背景色）

---

## 三、输出格式

根据目标平台决定：

### 公众号模式
- 纯内联样式 HTML，可直接粘贴到公众号编辑器
- 自动在顶部生成封面图区域（占位）
- 文末添加「往期推荐」「关注引导」板块
- 支持长截图分页

### 知乎模式
- 兼容知乎富文本编辑器的 HTML 子集
- 保留代码块和数学公式
- 自动添加引用来源标注

### 独立 HTML 模式
- 完整的 HTML 文档（含 `<head>` 样式和 `<meta>`）
- 响应式设计，移动端适配
- 可一键保存为 .html 文件

---

## 四、使用示例

**输入：**
```
# 标题

> 一句很酷的引言

正文内容……
```

**输出：**
```html
<section>
  <h1 style="font-size:22px;font-weight:700;color:#1a1a1a;margin:24px 0 12px;">标题</h1>
</section>
<section>
  <blockquote style="border-left:3px solid #007aff;padding:12px 16px;background:#f5f7fa;color:#515154;">
    一句很酷的引言
  </blockquote>
</section>
<section style="font-size:16px;line-height:1.75;color:#3c3c43;">
  正文内容……
</section>
```

---

## 五、经验沉淀

每次使用后 AI 自动询问排版效果，记录到本章：
- **有效策略**：哪种主题/字号组合阅读体验最好
- **踩坑记录**：哪个 HTML 元素在公众号显示异常
- **偏好调整**：字号、配色、间距等偏好变化
- **平台适配**：不同平台的兼容性问题
