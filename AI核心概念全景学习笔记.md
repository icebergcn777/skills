# AI 核心概念全景学习笔记

> 来源：视频字幕《AI Core Concepts Transcript》  
> 从 Token 到 Workspace Agent，一条主线串起所有 AI 热词

---

## 📍 主线一句话

AI 正在从一个"会聊天的工具"变成一个"能进入真实工作的数字员工"。每一个新概念的出现，都是在解决 AI 走向真实工作时遇到的一个具体问题。

---

## 第一章：Token & Context Window — AI 一次能看多少？

### Token 是什么？
AI 并不是像人一样直接阅读一整篇文章，它会先把输入内容拆成更小的信息单位，这个单位就叫 **Token**。

- 一个中文词可能被拆成几个 token
- 一个英文单词也可能被拆成几段
- 不同模型的拆分方式不完全一样

### Token 影响三个东西
1. **AI 一次能读多少内容？**
2. **调用 AI 的成本是多少？**（按 token 计费）
3. **AI 为什么会忘记前面聊过的内容？**

### Context Window（上下文窗口）
模型一次最多能处理多少 token，就叫 **Context Window**。

- 你的问题 + 历史对话 + 系统提示词 + 文件内容 + 工具返回结果，加起来不能超过这个范围
- 对话越来越长，前面的内容可能被"挤出去"——就像桌面只能摊十张纸，不断往上放新的，最早的就被拿走了
- 你看到的"128K 上下文"、"百万 token"、"长上下文模型"，本质上说的都是同一件事

### ⚠️ 重要提醒
**上下文窗口越大 = AI 一定越聪明吗？** 不一定。
- 信息太少 → AI 打不出来
- 信息太多 → AI 可能被无关内容干扰

真正好用的关键在于：**你怎么把任务交代清楚**。

---

## 第二章：Prompt Engineering — AI 怎么听懂任务？

### 为什么需要 Prompt？
同一个问题，不同问法，结果差别非常大。

❌ 随便问：*"帮我写个方案"* → 内容空洞（提升效率、优化体验、扩大影响力…）
✅ 结构化问：*"你是一名资深产品经理，请针对一个面向开发者的 AI 工具写一份产品方案，要求包含目标用户、核心痛点、功能模块、商业模式和落地路径，用表格输出"* → 具体、有结构、接近想要的东西

### Prompt Engineering 的本质
不是写咒语，而是**给 AI 写一份工作说明书**，需要包含：

| 要素 | 说明 |
|------|------|
| 角色 | 你现在扮演什么角色 |
| 任务 | 要完成什么任务 |
| 背景 | 背景信息是什么 |
| 格式 | 输出格式是什么 |
| 约束 | 哪些内容不要写 |
| 标准 | 什么结果才算好 |
| 示例 | 最好还能给几个例子 |

### Prompt 的边界
Prompt 再强，只能解决**任务怎么说清楚**，解决不了另一个致命问题：**模型不知道的东西，它就是不知道**。

- 让它总结你的内部文档 → 没看过
- 让它分析你的项目代码 → 没读过
- 让它回答昨天发生的事 → 训练时根本没有

如果硬让它回答 → 一本正经地胡说八道。

---

## 第三章：RAG — AI 怎么查资料？

### RAG 解决的核心问题
> 不要让 AI 凭记忆回答，先让它查资料，再让它回答。

**RAG = Retrieval Augmented Generation（检索增强生成）**

逻辑很简单：**先检索，再生成**。

### 工作流程
```
用户提问 → 去知识库搜索相关内容 → 把找到的内容交给 AI → AI 基于这些资料回答
```

### 举例
问："年假要提前几天申请？"
- ❌ 没有 RAG：AI 没看过员工手册，只能瞎猜
- ✅ 有 RAG：先在员工手册里搜索"请假申请"，找到相关条款，AI 回答："根据员工手册，年假需要提前三个工作日申请"

### RAG 背后的三个重要概念

| 概念 | 说明 |
|------|------|
| **Embedding（嵌入）** | 把文字变成一串数字 |
| **Vector Database（向量数据库）** | 专门存这些向量的地方，快速找相似内容 |
| **Knowledge Base（知识库）** | 存放资料的仓库 |

**为什么要把文字变成数字？** 计算机不懂"意思"，但可以比较数字之间的距离。比如"怎么申请退款"和"订单取消后钱怎么退"字面不同但意思接近，向量化后就能判断语义相似度。

### 从 RAG → Agentic RAG（进化方向）

| 维度 | 普通 RAG | Agentic RAG |
|------|----------|-------------|
| 工作方式 | 查一次 → 生成 | 像研究助理，会迭代 |
| 资料不够 | 不处理 | 换关键词继续查 |
| 问题太大 | 不处理 | 拆成子问题 |
| 冲突信息 | 不处理 | 做交叉对比 |

---

## 第四章：Tool Calling / Function Calling — AI 怎么从回答变成行动？

### 核心价值
> RAG 让 AI 有了资料，Tool Calling 让 AI 有了"手"。

AI 不再只是**生成文字**，它开始可以**调用外部工具**。

### 举例对比

| 场景 | 没有 Tool Calling | 有 Tool Calling |
|------|------------------|-----------------|
| 查订单状态 | "你可以去查一下订单系统"（建议） | 直接调用订单系统查询 |
| 跑 Python 代码 | 给你代码让你自己跑 | 直接调用执行环境运行 |
| 查日程 | "你可以看看日历" | 直接查日历告诉你下午的安排 |

### 带来的新问题
工具一多就乱了：
- 接天气要写一套
- 接数据库要写一套
- 接 GitHub 要写一套
- 接公司内部系统又要写一套

每个工具单独开发接口，每个平台有自己的接法，每个 agent 都要重复集成。→ 需要统一标准

---

## 第五章：MCP（Model Context Protocol）— AI 怎么统一连接外部世界？

### MCP 是什么？
> AI 连接外部工具和数据的**统一标准协议**，可以理解为 AI 世界里的 **USB-C**。

### 打个比方
以前：
- 手机一个接口
- 电脑一个接口
- 相机一个接口
- 充电器一个接口

USB-C 出现后，大家希望尽量用一个统一接口来连接。

### MCP 在 AI 世界做的事
以前每个 AI 应用接工具就像自己拉电线：
- 接 GitHub → 写一套
- 接数据库 → 写一套
- 接文件系统 → 写一套
- 接企业内部系统 → 再写一套

MCP 想解决的是：
- 工具怎么暴露给 AI？
- AI 怎么知道自己可以调用哪些能力？
- 工具需要哪些参数？
- 调用结果怎么返回？
- 权限和安全边界怎么控制？

### 定位
MCP 不是工具，它是 **AI 接入工具生态的连接层**。

---

## 第六章：Context Engineering — AI 每次到底该看什么？

### Prompt Engineering vs Context Engineering

| 维度 | Prompt Engineering | Context Engineering |
|------|------------------|-------------------|
| 关注点 | 这一句话怎么写更好 | 这次任务应该给 AI 准备哪些信息 |
| 范围 | 单条指令 | 整条信息流 |

### 问题的变化
AI 应用越来越复杂后，真正的问题是：
- 要不要看历史对话？
- 要不要看用户资料？
- 要不要看数据库结果？
- 要不要看上一次任务状态？
- 要不要看公司规范？
- 要不要看工具调用结果？

### 举例：AI 客服助手
用户说"帮我回复这个客户"：
- ❌ 简单 AI：只能根据当前一句话来回复
- ✅ 好用的 AI 需要知道：
  - 这个客户是谁？
  - 他之前买过什么？
  - 他之前投诉过什么？
  - 公司退款政策是什么？
  - 这次回复要不要升级给人工？

### 核心原则
> Context Engineering 的核心不是给 AI **更多**信息，而是给 AI **刚好需要**的信息。

- 太少了 → AI 判断不准
- 太多了 → AI 被干扰
- 信息过期了 → AI 做出错误判断
- 权限没管好 → 泄露敏感数据

### Context Engine（更产品化的概念）
Context Engineering 是方法，Context Engine 是系统：
- 该查哪些资料
- 该带哪些历史记录
- 该过滤哪些敏感信息
- 该压缩哪些长内容
- 该把重要信息放在前面

---

## 第七章：Skill — AI 怎么沉淀可复用能力？

### Skill 解决的核心问题
> **我不想每次都重新教 AI 一遍。**

### 举例：写周报
每次让 AI 帮忙写周报都要重新交代：
- 这周做了哪些事
- 哪些项目有进展
- 哪些问题还没解决
- 下周计划怎么写
- 语气正式但不空、不写成流水账、要突出成果

**有了 Skill 之后**：只需要说"按我的周报格式来写"，AI 就知道：
1. 先整理本周事项
2. 再提炼成果和问题
3. 按固定格式输出
4. 调整语气为公司汇报风格

### Prompt vs Skill

| 维度 | Prompt | Skill |
|------|--------|-------|
| 性质 | 一次性指令 | 长期能力 |
| 使用方式 | 每次写一次 | 保存下来反复调用 |
| 价值 | 让单次回答更好 | 沉淀一类工作方法 |

### Skill 的意义
- **个人**：把自己工作风格沉淀下来
- **团队**：把标准流程沉淀下来
- **企业**：把岗位经验变成可复用的 AI 能力

---

## 第八章：Computer Use — AI 怎么操作电脑？

### Tool Calling vs Computer Use

| 维度 | Tool Calling | Computer Use |
|------|-------------|-------------|
| 原理 | 调用 API | AI 像人一样操作电脑 |
| 适用场景 | 系统有 API | 系统没有 API |
| 操作方式 | 接口调用 | 看屏幕 → 理解界面 → 鼠标键盘操作 |

### 为什么需要 Computer Use？
现实中有大量系统没有 API，或者 API 非常难接：
- 老旧后台系统
- 网页管理系统
- 内部审批页面
- 只能人工点击的表单

### 具体操作能力
打开网页 → 点击按钮 → 输入内容 → 滚动页面 → 选择下拉框 → 提交表单 → 下载文件 → 上传资料

### 举例：报销发票
公司系统没有 API：
- ❌ 以前：自己打开网页 → 登录后台 → 点击报销 → 填写金额 → 上传发票 → 选择部门 → 提交审批
- ✅ 有 Computer Use：AI 像人一样操作浏览器 → 打开报销系统 → 识别发票金额 → 填写表单 → 上传文件 → 提交申请

### 当前难点
- 网页布局会变
- 按钮可能识别错
- 登录可能需要验证码
- 一步点错后面全错
- 涉及权限和安全

---

## 第九章：Agent — AI 怎么自己拆任务、循环执行？

### Agent 不是什么
❌ 更聪明的聊天机器人

### Agent 是什么
> Agent 真正的关键不是聊天，而是他能围绕一个目标**自己拆步骤、选工具、观察结果、再继续调整**。

### 三种模式对比

| 模式 | 工作方式 | 示例 |
|------|---------|------|
| 聊天机器人 | 你问一句，AI 答一句 | "帮我写一首诗" |
| Tool Calling | 你让做一个动作，它调一个工具 | "查一下天气" |
| **Agent** | 你给一个目标，它自己推进 | "分析项目为什么启动失败" |

### 举例：分析项目启动失败
Agent 会这样工作：
1. 看错误日志
2. 读配置文件
3. 检查依赖版本
4. 搜索代码中的相关调用
5. 尝试运行测试
6. 如果出现新报错 → 继续定位
7. 给出修改方案 → 甚至直接改代码

**核心循环**：计划 → 行动 → 观察结果 → 根据结果调整下一步

### Agent 为什么先在编程领域爆发？
代码特别适合 Agent：
- 文件结构明确
- 报错信息具体
- 测试结果能反馈对错
- 版本控制可记录改动
- 改完能自动运行验证

### Vibe Coding（新趋势）
> 以前是你写代码 AI 辅助，现在是你描述目标 AI 生成实现，你负责验收和调整。

程序员的工作重心在变化：
- 以前：花大量时间写具体实现
- 以后：花更多时间定义需求、拆解任务、设计架构、审核代码、控制质量、管理 AI Agent

---

## 第十章：Harness Engineering — Agent 怎么安全上线？

### 问题
Agent 越强，风险也越明显：
- 会犯错
- 会乱改文件
- 会误删内容
- 会生成不安全代码
- 会跑偏
- 会消耗大量 token
- 可能做出"看起来能跑但你完全不敢上线"的东西

### Harness Engineering 是什么
> 给 AI Agent 套上的**安全带、方向盘、仪表盘和刹车系统**。

**比喻**：如果模型是发动机，Harness 就是整辆车的控制系统。发动机越强，越需要控制——否则不是跑得更快，而是更容易出事故。

### 包含的内容

| 组件 | 说明 |
|------|------|
| 权限控制 | 哪些操作不能做 |
| 工具白名单 | 只能调用允许的工具 |
| 沙箱隔离 | 在隔离环境里执行 |
| 错误重试 | 出错了自动重试 |
| 输出验证 | 生成结果自动检查 |
| 人工审批 | 关键操作需要人确认 |
| 成本控制 | 防止 token 消耗失控 |
| 安全边界 | 防止越权访问数据 |
| 回滚机制 | 出问题能恢复 |
| 评测系统 | 持续评估 Agent 表现 |

### 举例对比

**整理电脑文件：**
- ❌ 无 Harness → 可能误删重要文件
- ✅ 有 Harness → 限制只能访问某文件夹，所有操作都记录，危险操作需要人工审批

**改代码：**
- ❌ 无 Harness → 可能直接改生产代码
- ✅ 有 Harness → 只能在沙箱里改，改完必须跑测试，通过后才能提交，提交前还要人工 review

### 核心变化
> 难点正在从**模型够不够聪明**转向**系统够不够可靠**。

---

## 第十一章：Workflow — AI 怎么进入业务流程？

### 为什么需要 Workflow？
企业工作不是一个 Agent 单独完成的，真实工作往往是一条**流程**。

### 举例：客户提交咨询表单
1. 读取表单
2. 判断客户类型
3. 查询 CRM
4. 生成跟进建议
5. 分配销售
6. 发送邮件通知
7. 群聊通知
8. 等待人工确认
9. 写入数据库
10. 生成日报
11. 后续继续跟进

这不是一次聊天，也不是一个工具调用，这是一条**业务流程**。

### Agent vs Workflow

| 维度 | Agent | Workflow |
|------|-------|----------|
| 职责 | 思考和判断 | 把步骤按顺序串起来 |
| 工作方式 | 自主决策 | 按设计好的步骤执行 |
| 典型工具 | Claude, GPT | n8n, Dify, Zapier, Make, LangGraph |

### Workflow 的价值
- 把 AI、API、数据库、消息系统、人工审批、定时任务、条件判断串起来
- AI 只负责一部分判断和生成
- 真正让整个事情跑起来的是 Workflow
- 普通人不需要从零写系统，可以像搭积木一样把现有工具串起来

---

## 第十二章：Workspace Agent — AI 怎么变成数字员工？

### Workflow vs Workspace Agent

| 维度 | Workflow | Workspace Agent |
|------|----------|----------------|
| 性质 | 一条流程 | 一个长期待在团队里的岗位助手 |
| 工作方式 | 设计好步骤，按步骤执行 | 理解团队长期积累的上下文 |
| 时长 | 执行完就结束 | 长期存在于工作空间 |

### Workspace Agent 知道的
- 团队有哪些项目
- 文档放在哪里
- 谁负责什么
- 这个客户之前发生过什么
- 哪个任务卡住了
- 哪些信息是敏感的
- 哪些操作需要审批
- 谁有权限看什么
- 什么时候应该提醒
- 什么时候应该交给人类

### 举例：写项目周报

**普通 Agent：**
→ "项目内容是什么？进度是什么？风险是什么？下周计划是什么？"
→ 因为它不知道团队发生了什么

**Workspace Agent：**
→ 它已经在工作空间里，能看到任务系统、代码提交、会议纪要、文档更新、成员分工、上周周报
→ 自动整理出本周完成了什么、哪些任务延期了、哪个模块风险最大、下周该推进什么

### 产品形态变化
AI 不再是**临时打开的工具**，开始变成团队工作空间里长期存在的一类角色：

- AI 销售助理
- AI 数据分析助手
- AI 项目管理助手
- AI 研发助手

他们不再只是回答问题，而是**长期处理某一类工作**，并和团队流程结合在一起。

### 面临的现实问题
- 权限怎么管？
- 数据怎么隔离？
- 不同成员看到的信息不一样怎么办？
- AI 建议错了谁负责？
- 什么时候自动执行，什么时候必须人工确认？
- 长期记住的信息哪些保留，哪些过期？

---

## 🗺️ 全图总览：AI 热词背后的主线

```
Token / Context Window ─── AI 能看多少？
        ↓
Prompt Engineering ─────── 怎么把任务说清楚？
        ↓
RAG ────────────────────── 怎么查资料？
        ↓
Tool Calling ───────────── 怎么调用工具？
        ↓
MCP ────────────────────── 怎么统一连接外部？
        ↓
Context Engineering ────── 每次该看什么信息？
        ↓
Skill ──────────────────── 怎么沉淀可复用能力？
        ↓
Computer Use ───────────── 怎么操作电脑？
        ↓
Agent ──────────────────── 怎么自己拆任务执行？
        ↓
Harness Engineering ────── 怎么安全上线？
        ↓
Workflow ───────────────── 怎么进入业务流程？
        ↓
Workspace Agent ────────── 怎么变成数字员工？
```

## 💡 核心心法

> **以后看到新的 AI 热词，不要急着背定义，只需要问一个问题：**
> **"它到底是在解决 AI 走向真实工作时的哪个问题？"**
>
> 这个问题想清楚了，再多的新概念也不会乱。

---

*整理于 2026-06-18，基于视频字幕学习笔记*
