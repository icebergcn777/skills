# Cowork → Claude Code 规则同步

> 🔄 全量同步于 2026-07-10 | 源：Cowork instinct-store.md + memory-decay-rules.md
> 自动同步 Skill：`/sync-instincts`
> 供 **Claude Desktop 内的 Claude Code（CLI 终端模式）** 使用。
> 当源文件更新时，本文件会被全量覆盖式同步。

---

## 安装方式

| 应用范围 | 路径 | 效果 |
|---------|------|------|
| **所有项目全局** | `~/.claude/rules/cowork-instincts.md` | 全局 Claude Code 会话生效 |
| **当前项目** | `<项目根>/.claude/rules/cowork-instincts.md` | 仅该项目生效 |
| **嵌入指令** | 把以下全部内容贴入 `~/.claude/CLAUDE.md` | 全局指令级生效 |

---

## 共享规则

### R1. 全流程分析走三级递进

**场景：** 需要深度分析时（股票分析、项目评估、商业决策）
**规则：** 走 L1(速查) → L2(定量) → L3(尽调+对抗审查) 三级流水线，不跳过中间验证环节
- L1：快速扫描基本面、行情事件、关键指标
- L2：估值建模、对标分析、量化评分
- L3：多角度压力测试、认知谬误检测、交叉验证 + 置信度评分
**反例：** 直接跳到最终结论，跳过中间验证

### R2. 内容创作标准流水线

**场景：** 写文章、公众号、小红书、视频文稿
**规则：** 按序执行：写作 → 去AI味（删掉套路化表达）→ 排版适配目标平台 → 配封面图 → 发布
**注意：** 每一步输出是下一步的输入，不要跳步

### R3. 涨停分析三板斧

**场景：** 分析 A 股涨停股
**规则：** 三步全部完成后再出结论
1. 竞价评分 + 涨停模式识别（什么类型涨停？量价配合？真假？）
2. 风险排查（查减持公告、近期利空、龙虎榜席位）
3. 量价确认（量柱形态、量线支撑压力位、量波走势验证）

### R4. 任务编排原则

**场景：** 安排多个子任务时
**规则：**
- 先检查任务间是否有依赖关系
- 有依赖 → 串行跑，前一产出作为后一输入
- 无依赖 → 独立任务并行执行
- 切换到新任务前确认当前任务已完结
**反例：** 所有任务揉在一起同时做、做到一半切出去

### R5. 置信度衰减原则

**场景：** AI 记住的规则/事实需要判断可信度
**规则：**
- 每条规则/事实带置信度分数和最后验证时间
- 超过 30 天未验证的规则，置信度按半衰期衰减一半
- 有效分 ≥ 0.7 直接采纳，0.2-0.49 标记"旧信息"，< 0.2 跳过
- 当前任务匹配规则领域时（如分析股票匹配交易规则），可提升衰减记忆的优先级

### R6. 项目隔离原则

**场景：** 跨项目工作时
**规则：** 不同项目积累的规则/偏好默认隔离。React 项目的命名习惯不自动应用于 Python 项目。同一模式在 2+ 个项目中同时出现时，提升为通用规则。

### R7. Instinct 定期导出

**场景：** instinct-store 有新增/变更时
**规则：** 出现新规则或已有规则置信度跨越阈值时，同步更新 codex-instinct-rules.md 和 COWORK-CODEX-RULES.md
**注意：** 此规则由 `/sync-instincts` Skill 自动执行。手动调用方式：直接说"同步一下"。

---

## 同步记录

| 同步时间 | 源版本 | 变更内容 |
|---------|--------|---------|
| 2026-07-10 | 初始全量 | instinct-store 6条 + memory-decay-rules R5/R6 全量导出 |
| 2026-07-10 | 自动同步 v2 | 新增 R7 (instinct-export-to-shared)；新建 `/sync-instincts` Skill |

## 内容优先级

以上规则来源于 Cowork chat 环境的 instinct 自动观测系统。如果项目已有 CLAUDE.md 或具体指令，以项目指令为准。

## 更新维护

本文件随 `/sync-instincts` Skill 自动更新。检查顶部日期确认最新版本。
